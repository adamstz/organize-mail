fastapi>=0.100
uvicorn>=0.22
websockets>=12.0  # WebSocket support for real-time features
google-api-python-client
google-auth-oauthlib
google-cloud-pubsub
pydantic
httpx  # Required for FastAPI TestClient
psycopg2-binary>=2.9.0  # PostgreSQL adapter

# LLM providers (optional - install what you need)
openai>=1.0.0
anthropic>=0.18.0

# LangChain for RAG and LLM orchestration
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-anthropic>=0.1.0
langchain-community>=0.0.20
langchain-ollama>=0.1.0  # New Ollama integration (replaces deprecated ChatOllama)

# RAG and embeddings
sentence-transformers>=2.2.0  # Local embedding models
pgvector>=0.2.0  # PostgreSQL vector extension Python client

# Email security and sanitization
bleach>=6.0.0  # HTML sanitization
beautifulsoup4>=4.12.0  # HTML parsing and manipulation
tinycss2>=1.2.0  # CSS parsing for sanitization
